# Interfaces Module

Abstract protocols and interfaces for clean architecture.

High-level modules depend on abstractions here, not on concrete implementations.

## Components

- `strategy.py`: StrategyInterface
- `benchmark.py`: EnvAdapterInterface
- `data_loader.py`: DataLoaderInterface
